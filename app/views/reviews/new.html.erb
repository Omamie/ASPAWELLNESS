<!-- <div class="container py-3 ">
  <div class="modal-card-smaller p-3">
    <div>
      <h1 class="mx-3 my-3"> Register your center </h1>
      <div>
        <%= simple_form_for [@center, @treatment, @booking, @review] do |f| %>
        <%= f.input :stars %>
        <%= f.input :description %>
        <%= f.submit "Review", class: "btn btn-primary" %>
      </div>
      <% end %>
    </div>
  </div>
 -->
